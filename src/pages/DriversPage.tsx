import React from "react";
import StubPage from "./StubPage";
export default function DriversPage() {
  return (
    <StubPage
      title="Drivers"
      icon="DirectionsCar"
      description="Manage driver accounts, documents, status, and assignments."
    />
  );
}
