import React from "react";
import StubPage from "./StubPage";
export default function TripsPage() {
  return (
    <StubPage
      title="Trips"
      icon="Route"
      description="View live and historical trip data, routes, statuses, and fares."
    />
  );
}
