import React from "react";
import StubPage from "./StubPage";
export default function NotificationsPage() {
  return (
    <StubPage
      title="Notifications"
      icon="Notifications"
      description="View and manage all broadcast and push notifications."
    />
  );
}
