import React from "react";
import StubPage from "./StubPage";
export default function RidersPage() {
  return (
    <StubPage
      title="Riders"
      icon="PersonPin"
      description="Manage all riders — view profiles, trip history, wallet balances, and more."
    />
  );
}
