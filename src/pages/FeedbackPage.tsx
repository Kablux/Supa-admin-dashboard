import React from "react";
import StubPage from "./StubPage";
export default function FeedbackPage() {
  return (
    <StubPage
      title="Feedback"
      icon="Feedback"
      description="Review user feedback, ratings, and platform suggestions."
    />
  );
}
